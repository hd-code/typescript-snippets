import assert from 'assert';
import { Matrix } from '../src/math';

// -----------------------------------------------------------------------------

describe('math_matrix', () => {
    it('isMatrix()', () => {});
    
    it('transpose()', () => {});

    it('add()', () => {});

    it('sub()', () => {});

    it('mul()', () => {});

    it('scale()', () => {});

    it('mulVector()', () => {});

    it('round()', () => {});
});