{
  "name": "web-snippets",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "npm run clear && tsc -p tsconfig.build.json && npm run minify",
    "check": "tsc --noEmit",
    "clear": "rm -rf js/*",
    "deploy": "npm run format && npm run check && npm test && npm run build",
    "format": "prettier --write src test",
    "minify": "for i in $(find js -name *.js); do uglifyjs $i -o $i -mc --comments '/^!/'; done;",
    "start": "npm run deploy",
    "test": "mocha -r ts-node/register -r tsconfig-paths/register -p 'test/**/*.ts'"
  },
  "devDependencies": {
    "@types/mocha": "^9.0.0",
    "@types/node": "^14.17.17",
    "mocha": "^9.1.1",
    "prettier": "^2.4.1",
    "ts-node": "^10.2.1",
    "tsconfig-paths": "^3.11.0",
    "typescript": "^4.4.3",
    "uglify-js": "^3.14.2"
  }
}
