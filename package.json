{
  "name": "js-snippets",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "npm run clear && tsc && npm run minify",
    "clear": "rm -rf ./build",
    "minify": "for i in $(find build -name *.js); do uglifyjs $i -o $i -mc; done;",
    "start": "npm test",
    "test": "mocha -r ts-node/register 'test/**/*.ts'"
  },
  "devDependencies": {
  }
}
