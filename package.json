{
  "name": "hd-snippets-js",
  "version": "0.2.0",
  "description": "This repo just contains a collection of very helpful JS functions that I use again and again in several projects. There are browser as well as node.js snippets.",
  "homepage": "https://github.com/hd-code/hd-snippets-js#readme",
  "bugs": {
    "url": "https://github.com/hd-code/hd-snippets-js/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/hd-code/hd-snippets-js"
  },
  "license": "MIT",
  "author": "Hannes Dröse <office.hd@gmx.net>",
  "files": [
    "build"
  ],
  "scripts": {
    "build": "npm run clear && tsc && npm run minify",
    "clear": "rm -rf ./build",
    "minify": "for i in $(find build -name *.js); do uglifyjs $i -o $i -mc --comments /^!/; done;",
    "start": "npm test",
    "test": "mocha -r ts-node/register 'test/**/*.ts'"
  },
  "devDependencies": {
    "@types/mocha": "^7.0.2",
    "@types/node": "^12.12.32",
    "mocha": "^7.1.1",
    "ts-node": "^8.8.1",
    "typescript": "^3.8.3",
    "uglify-js": "^3.8.1"
  }
}
